<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="guestbook">
	<select id="findAll" resultType="boardvo">
		<![CDATA[	
		  "select a.no, a.title, a.contents, a.reg_date, a.hit, a.group_no, a.order_no, a.depth, a.user_no, b.name 
		  from board a, user b 
		  where a.user_no = b.no order by group_no desc, order_no asc";
	]]>
	</select>
	
	<insert id="insert" parameterType="boradvo">
		<![CDATA[
		insert
		  into guestbook
		values (null, #{title }, #{contents}, #{password }, now())	
	  	]]>
	  	<selectKey keyProperty="no" resultType="long" order="AFTER">
			<![CDATA[
		  	select last_insert_id()
	  		]]>
	  	</selectKey>	
	</insert>
	
	<delete id="delete" parameterType="guestbookvo">
		<![CDATA[
		delete
		  from guestbook
		 where no=#{no }
		   and password=#{password }
	  	]]>	
	</delete>
</mapper>